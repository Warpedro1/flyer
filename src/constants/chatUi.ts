/** Tempo simulado entre a mensagem do utilizador e a seguinte do assistente (onboarding). */
export const SIMULATED_ONBOARDING_ASSISTANT_DELAY_MS = 1_100;

/**
 * Tempo mínimo desde o envio até mostrar a resposta da API no chat,
 * para evitar flashes quando a rede responde muito depressa.
 */
export const MIN_CHAT_ASSISTANT_DISPLAY_MS = 750;
